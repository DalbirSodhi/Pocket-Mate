# Supabase Edge Functions

Server-only backend endpoints will live here when a feature should not be handled directly from the mobile app.

Examples:

- dashboard summaries that need stricter server logic
- future external API calls
- future exports
- future AI or storage operations
